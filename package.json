{
  "name": "employee-mngmt-be",
  "version": "1.0.0",
  "description": "employee managment system backend and apis",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node dist",
    "start:dev": "nodemon --watch .env --watch src --exec babel-node src --source-maps",
    "build": "rimraf dist && babel src --copy-files --out-dir dist --source-maps"
  },
  "author": "Ashan Tharindu",
  "license": "ISC",
  "devDependencies": {
    "@babel/cli": "^7.19.3",
    "@babel/core": "^7.19.3",
    "@babel/preset-env": "^7.19.4",
    "nodemon": "^2.0.20"
  },
  "dependencies": {
    "@babel/node": "^7.19.1",
    "axios": "^1.1.2",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "express-http-context": "^1.2.4"
  }
}
